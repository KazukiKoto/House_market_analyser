# Database Configuration
# DB_DEFAULT=/app/properties.db

# Ollama Configuration
# If using Ollama on host machine (default)
OLLAMA_HOST=http://host.docker.internal:11434

# If using Ollama as a separate Docker container
# OLLAMA_HOST=http://ollama:11434

# Port Configuration (only needed if you want to change the default)
# PORT=8000
