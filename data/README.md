# Data Directory

This directory is used to persist the SQLite database when running in Docker.

When you run the application with Docker Compose, any database files created will be stored here on your host machine, ensuring data persistence across container restarts.
